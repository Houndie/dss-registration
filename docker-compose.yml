version: "3"
services:
  db: 
    build:
      context: dynamic
      dockerfile: docker/Dockerfile.postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=dbuser
      - POSTGRES_PASSWORD=dbpass
  test_db: 
    build:
      context: dynamic
      dockerfile: docker/Dockerfile.postgres
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=dbuser
      - POSTGRES_PASSWORD=dbpass
  backend:
    build: 
      context: dynamic
      dockerfile: docker/Dockerfile
    ports:
      - "8080:80"
    environment:
      - DSS_FRONTEND=http://localhost:8081
      - DSS_POSTGRESURL=postgres://dbuser:dbpass@db:5432/dbuser?sslmode=disable
      - DSS_TEST_POSTGRESURL=postgres://dbuser:dbpass@test_db:5432/dbuser?sslmode=disable
      - DSS_ENVIRONMENT=development
      - DSS_SQUAREKEY
      - DSS_MAILKEY
      - DSS_RECAPTCHAKEY
      - DSS_PORT=80
      - DSS_ADMINS
    volumes:
      - "./dynamic/:/app:ro"
  frontend:
    build:
      context: static
      dockerfile: docker/Dockerfile
    environment:
      - GATSBY_BACKEND=http://localhost:8080/
      - GATSBY_FRONTEND=http://localhost:8081/
      - GATSBY_CLIENT_ID=166144116294-c115t8bqllktva4qp6tvjjeqe7mdggu3.apps.googleusercontent.com
    volumes:
      - "./static/gatsby/src:/app/src:ro"
      - "./static/gatsby/static:/app/static:ro"
    ports:
      - "8081:8000"
