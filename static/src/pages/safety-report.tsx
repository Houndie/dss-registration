import React, {useEffect, useState} from 'react'
import Page from '../components/Page'

export default () => (
	<Page title="Safety Report">
		{() => (
			<p>This page will become active during the event!</p>
		)}
	</Page>
)
