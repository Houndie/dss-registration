import React from 'react'
import Page from '../components/Page'

export default () => (
	<Page title="Registration">
		{() => (
			<p>Registration is not open yet!  Stay tuned for more information</p>
		)}
	</Page>
)
